{
  "name": "pollaris-release-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test src/users.test.js src/products.test.js src/liberar-pending.test.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
